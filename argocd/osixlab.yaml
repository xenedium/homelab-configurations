apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: osixlab
  namespace: argocd
  annotations:
    argocd-image-updater.argoproj.io/image-list: >-
      frontend=registry.osixlab.com/osixlab/osixlab-frontend:latest,
      backend=registry.osixlab.com/osixlab/osixlab-backend:latest
    argocd-image-updater.argoproj.io/frontend.update-strategy: digest
    argocd-image-updater.argoproj.io/backend.update-strategy: digest
    argocd-image-updater.argoproj.io/frontend.pull-secret: pullsecret:osixlab/registry-osixlab
    argocd-image-updater.argoproj.io/backend.pull-secret: pullsecret:osixlab/registry-osixlab
spec:
  project: default
  source:
    repoURL: https://github.com/xenedium/homelab-configurations.git
    path: apps/osixlab
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: osixlab
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
