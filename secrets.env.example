# Copy this file to secrets.env and fill in the values.
# secrets.env is gitignored and never committed.

# Cloudflare API token — used by both cert-manager and external-dns
# Required permissions: Zone.Zone:Read, Zone.DNS:Edit
CF_API_TOKEN=
